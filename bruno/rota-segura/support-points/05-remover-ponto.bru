meta {
  name: Remover ponto de apoio
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/support-points/{{supportPointId}}
  body: none
  auth: none
}

tests {
  test("deve retornar 204", function() {
    expect(res.status).to.equal(204);
  });
}
